OBS para funcionar em docker, é necessário 
código (/app)
docker-compose.yml
Dockerfile
e rodar docker compose up

Se remover isso do docker compose yml, aí a imagem é completa
volumes:
  - ./app:/app

Aí sim executar o build final
docker build -t rock_ai-rocks:latest .

E o exportar imagem
docker save -o rocks.tar rock_ai-rocks:latest

No outro pc
docker load -i rocks.tar
docker run -it rock_ai-rocks


docker compose up --build
docker exec -it ollama ollama pull gemma4:e2b
docker exec -it ollama ollama run gemma4:e2b

curl http://localhost:11434
docker compose run --rm rocks

Projeto
1- Estrutura Inicial
    mkdir rocks_ai
    cd rocks_ai

2 - Estrutura base
    mkdir app
    touch app/main.py
    touch requirements.txt
    touch Dockerfile
    touch docker-compose.yml
    touch .env

o docker compose up, cria imagem, inicia o conteiner, mas deixa ele preso, sem interação comigo, mas o docker compose run rocks, faz eu interagir diretamente no container.


Isso aqui faz criar o container e ao sair exluir ele
docker compose run --rm rocks


################################################### Iniciar o Projeto ############################################
Criar imagem
rodar docker compose up

docker compose up --build
docker compose up --build -d

entrar no container já ativo
docker exec -it rocks_app sh

Conectar no app.py 
docker attach rocks_app

Conectar no chat
docker exec -it rocks_app python interfaces/web/app.py

sair sem matar o container
CTRL + P + Q

Iniciar o conteiner novamente
docker start rocks_app

rocks > usar qwen
rocks > usar ollama
rocks > usar gemini


Observar logs

docker compose logs -f ollama
docker compose logs --tail=50 ollama


cli docker exec -it rocks_app python interfaces/cli/cli.py
webchat - localhost:5000

rocks > sair


# Subir tudo em background
docker compose up -d

# Entrar no CLI
docker exec -it rocks_app python interfaces/cli/cli.py

# Ver logs se precisar
docker compose logs -f rocks_app

# Desligar tudo
docker compose stop